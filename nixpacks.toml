# Nixpacks 빌드 설정
# Railway Variables(ENV)가 빌드 시점에 자동으로 주입됩니다.
# REACT_APP_API_URL 을 Railway Variables 탭에서 설정하면 CRA 빌드에 자동 포함됩니다.

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["CI=false npm run build"]

[start]
cmd = "npx serve -s build -l 3000"
